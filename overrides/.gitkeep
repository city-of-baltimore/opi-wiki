# Keeps the MkDocs Material `custom_dir: overrides` path present in a clean
# checkout. Git does not track empty directories, so without this file the
# strict build aborts with "custom_dir does not exist". Remove once real
# template overrides live here.
